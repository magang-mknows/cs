import { FC, ReactElement } from "react";
import Button from "../atoms/button";

const Card: FC = (): ReactElement => {
  return (
    <section>
      <Button />
    </section>
  );
};

export default Card;
