import { FC, ReactElement } from "react";

const SkorSection: FC = (): ReactElement => {
  return (
    <section className="bg-white h-auto w-[50%] m-8">
      <h1 className="text-xl font-semibold p-4">RIWAYAT NILAI SKOR</h1>
    </section>
  );
};
export default SkorSection;
